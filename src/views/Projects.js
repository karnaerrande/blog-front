import React from 'react';
import { Container } from 'react-bootstrap';

const projStyle = {
    textAlign:'center'
}

export default function Projects(props) {
    return (
        <Container style={projStyle}>
            <h1>Projects</h1>
        </Container>
    );
}